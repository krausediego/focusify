import { Text, View } from 'react-native';

const SettingsPage: React.FC = () => {
  return (
    <View className="flex-1 relative bg-white items-center justify-center">
      <Text>Settings</Text>
    </View>
  );
};

export { SettingsPage };
